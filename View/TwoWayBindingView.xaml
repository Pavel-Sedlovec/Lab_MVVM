<UserControl x:Class="Lab_MVVM.View.TwoWayBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lab_MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <GroupBox Header="Привязка Элемент-Элемент"
                  Margin="0,0,0,20">
            <StackPanel>
                <Slider Name="mySlider"
                        Minimum="0"
                        Maximum="100" />
                <TextBox Text="{Binding ElementName=mySlider, Path=Value, UpdateSourceTrigger = PropertyChanged}" />
                <TextBlock Text="Двигай слайдер — текст меняется, пиши цифры — слайдер ползет."
                           TextWrapping="Wrap"
                           Opacity="0.5" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Привязка через ViewModel">
            <StackPanel>
                <TextBox Text="{Binding TextTWB, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="{Binding TextTWB}"
                           Foreground="Blue"
                           FontSize="16" />
                <TextBlock Text="Данные проходят через C# код (ViewModel)"
                           Opacity="0.5" />
            </StackPanel>
        </GroupBox>
    </StackPanel>
</UserControl>
