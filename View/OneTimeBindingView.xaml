<UserControl x:Class="Lab_MVVM.View.OneTimeBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Lab_MVVM.View"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <StackPanel Margin="20">
        <TextBlock Text="{Tr 'OneTimeBindingView.Title'}"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="DarkBlue" />

        <StackPanel Orientation="Horizontal"
                    Margin="0,10">
            <TextBlock Text="{Tr 'OneTimeBindingView.TextLabel'}" />
            <TextBlock Text="{Binding TextOTB, Mode=OneTime}"
                       FontWeight="Bold" />
        </StackPanel>

        <TextBlock Text="{Tr 'OneTimeBindingView.Description'}"
                   TextWrapping="Wrap"
                   Opacity="0.6"
                   FontStyle="Italic" />
    </StackPanel>
</UserControl>